class wordToLearn {
  constructor(wordLevel, id, word, description, wordLength) {
    this.id = id;
    this.word = word;
    this.description = description;
    this.imageUrl = imageUrl;
    this.wordLevel = wordLevel;
  }
}

export default wordToLearn;
